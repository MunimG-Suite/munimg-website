# Tests As Behavior

Source root: `munimg-website`

Tests are indexed as behavior evidence because they often encode business
rules more explicitly than production files.

No tests detected by the deterministic extractor.
